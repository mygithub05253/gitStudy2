병합
=> 나눠진 브랜치를 하나의 항목으로 합치는 행위

1. fast-forward 기법
=> 메인(마스터) 브랜치, 그 외 다른 브랜치가 있을 때, 메인 브랜치에는 수정 사항이 없고, 다른 브랜치에 수정 사항이 있는 경우 이를 main이라는 브랜치로 합치는 행위

2. 3-way 기법
=> 동시에 여러 브랜치에서 수정이 발생하고 그 결과물을 하나로 합치는 행위

git merge 브랜치명
=> 지금 위치한 브랜치를 중심으로 해당 브랜치와 병합을 진행

git branch -d 브랜치명
=> 해당 브랜치를 삭제